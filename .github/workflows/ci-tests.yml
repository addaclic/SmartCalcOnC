name: C/C++ CI

on:
  push:
    branches: [ "master" ]

jobs:
  RunTests:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: install package
      run: apt install pkg-config check

    - name: Tests
      run: make test
      
